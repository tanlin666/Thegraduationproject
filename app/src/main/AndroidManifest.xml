<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tlandhmy.bueatifullife">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activity.NotificationmessageActivity"></activity>
        <activity android:name=".view.activity.NewfucosActivity" />
        <activity android:name=".view.activity.ReceivedcommentsActivity" />
        <activity android:name=".view.activity.WonthepraiseActivity" />
        <activity android:name=".view.activity.VideoDetailsActivity" />
        <activity android:name=".view.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.activity.LoginActivity"
            android:label="@string/title_activity_home" />
        <activity android:name=".view.activity.HomeDetailsActivity" />
    </application>

</manifest>